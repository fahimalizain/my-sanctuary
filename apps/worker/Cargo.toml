[package]
name = "sanctuary-worker"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
api-core = { path = "../../packages/api-core" }
worker = "0.8"

[build-dependencies]
serde_json = "1"
