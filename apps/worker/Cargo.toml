[package]
name = "sanctuary-worker"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
api-core = { path = "../../packages/api-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
worker = "0.8"

[build-dependencies]
serde_json = "1"
